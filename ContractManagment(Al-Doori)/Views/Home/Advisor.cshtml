@model AdvisorViewModel
@section Styles {
    <link rel="stylesheet" href="~/css/indexPage.css" />
}


<div class="IndexPageWrapper">

<div class="text-center">
    <div class="filteringGroups">
    <!--Search Form-->
        <form class="searchForm" method="post" asp-action="SearchActionAdvisor">
             <input type="search" class="searchBar" placeholder="Enter Advisor Name.." asp-for="@Model.AdvisorSearchName"/>
             <input type="submit" class="submitButton" value="Search" />
            <div class="errorDiv">
              <span class="ErrorSearchServerSide" asp-validation-for="@Model.AdvisorSearchName"></span>
             </div>
        </form>

    <!--Filter Form-->
        <form class="searchForm" method="post" asp-action="FilterActionAdvisor">
             
             <select class="filterSelectBox" asp-for=@Model.filterByChoice>
                 <optgroup label="Filter By">
                    
                     <option>Name</option>
                     <option>SurName</option>
                     <option>Age</option>
                 </optgroup>
             </select>
             <input type="submit" class="submitButton" value="Filter" />
            <div class="errorDiv">
               <!--If wanted provide validation for select box -->
             </div>
        </form>

         <!--Export Data Form-->
        <form class="searchForm" method="post" asp-action="ExportAdvisorsToExcel">
             <input type="submit" class="submitButton" value="Export Advisors To CSV or .xlsx sheet" />
            <div class="errorDiv">
               <!--If wanted provide validation for select box -->
             </div>
        </form>


    </div>
    <!--Each Contract will have it is own table and Own Details of Advisors and Adminstrators-->
  

        
            <div class="tableDetailsWrapper"> 
             <table class="table">
        <thead>
          <tr>
            <th>Name</th>
            <th>SurName</th>
            <th>Email</th>
            <th>Phone Number</th>
            <th>NIN</th>
            <th>Age</th>
          </tr>
        </thead>

        <tbody>
              @{
          for(int i =0;i < Model.advisors.Count; i++)
        {
          <tr>
            <th>@Model.advisors[i].Name</th>

            <th>
              @Model.advisors[i].SurName
            </th>

            <th>@Model.advisors[i].Email</th>

            <th>
             @Model.advisors[i].PhoneNumber
            </th>

            <th class="testCaseStepsCell">
             @Model.advisors[i].NationalIdentificationNumber
            </th>

            <th>
              @Model.advisors[i].Age
            </th>

          </tr>
          }
          }
        </tbody>
      </table>
            </div>
 
</div>
</div>